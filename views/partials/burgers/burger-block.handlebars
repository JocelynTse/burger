<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-ate={{devoured}}>
		{{#if devoured}}Devoured!{{else}}I want to eat this burger!!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete</button>
</li>
